module.exports = {
	processRequest: function(req){
		req.wants = function(){
			var accept = req.getHeader('Accept') || '';
			if (accept.indexOf('html') !== -1) {
				return 'html';
			};
			if (accept.indexOf('json') !== -1 || accept.indexOf('javascript') !== -1) {
				return 'json';
			};
			if (accpet.indexOf('xml') !== -1) {
				return 'xml';
			};
		}
	},
	type:'request'
}